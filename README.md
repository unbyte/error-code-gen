# Error Code Generator

```shell
usage: error-code-gen -i INPUT -t TEMPLATE OUTPUT

  -i, --input string
        path of the template to be used.
  -t, --template string
        path of the template to be used.
  output
        path of the rendered file.
```